export default {
      $baseFontTitle: '36rpx',
	  $baseFontContent: '28rpx',
	  $baseFontTime: '20rpx',
      setBaseFontTitle($baseFontTitle){
        this.$baseFontTitle= $baseFontTitle;
      },
	  setBaseFontContent($baseFontContent){
	    this.$baseFontContent= $baseFontContent;
	  },
	  setBaseFontTime($baseFontTime){
	    this.$baseFontTime= $baseFontTime;
	  }
}